# AWS CLI : S3

Retrieve the latest versiond
```bash
aws s3api list-object-versions \
  --bucket {bucket} \
  --prefix {prefix} \
  --no-paginate \
  --query 'Versions[0].VersionId' --output=text
```
